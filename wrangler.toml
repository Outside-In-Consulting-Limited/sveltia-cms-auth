name = "sveltia-cms-auth"
main = "src/index.js"
compatibility_date = "2023-03-24"

# Runtime vars declared here so `wrangler deploy` (the CI deploy) always ships them.
# These two are NOT sensitive (an OAuth client ID is public; the domain is public).
# GITHUB_CLIENT_SECRET is set as an encrypted SECRET in the Cloudflare dashboard and is
# preserved across deploys - do NOT put the secret here.
[vars]
GITHUB_CLIENT_ID = "Ov23li3wAhHEzknaCQLp"
ALLOWED_DOMAINS = "flipside.outin.nz"
